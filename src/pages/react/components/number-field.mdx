import { DocsLayout } from '@/components/DocsLayout';
import NumberFieldProps from '@/generated/props/NumberFieldProps';

export default ({ children }) => (
  <DocsLayout
    title="NumberField — Cladd"
    description="NumberField component props."
  >
    {children}
  </DocsLayout>
);

# NumberField

## Props

<NumberFieldProps />
