import { DocsLayout } from '@/components/DocsLayout';
import ToastProps from '@/generated/props/ToastProps';

export default ({ children }) => (
  <DocsLayout title="Toast — Cladd" description="Toast component props.">
    {children}
  </DocsLayout>
);

# Toast

## Props

<ToastProps />
