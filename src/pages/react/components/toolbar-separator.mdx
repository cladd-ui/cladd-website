import { DocsLayout } from '@/components/DocsLayout';
import ToolbarSeparatorProps from '@/generated/props/ToolbarSeparatorProps';

export default ({ children }) => (
  <DocsLayout
    title="ToolbarSeparator — Cladd"
    description="ToolbarSeparator component props."
  >
    {children}
  </DocsLayout>
);

# ToolbarSeparator

## Props

<ToolbarSeparatorProps />
