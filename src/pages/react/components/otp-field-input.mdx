import { DocsLayout } from '@/components/DocsLayout';
import OTPFieldInputProps from '@/generated/props/OTPFieldInputProps';

export default ({ children }) => (
  <DocsLayout
    title="OTPFieldInput — Cladd"
    description="OTPFieldInput component props."
  >
    {children}
  </DocsLayout>
);

# OTPFieldInput

## Props

<OTPFieldInputProps />
