import { DocsLayout } from '@/components/DocsLayout';
import LinkProps from '@/generated/props/LinkProps';

export default ({ children }) => (
  <DocsLayout title="Link — Cladd" description="Link component props.">
    {children}
  </DocsLayout>
);

# Link

## Props

<LinkProps />
