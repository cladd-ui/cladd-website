import { DocsLayout } from '@/components/DocsLayout';
import SwitchProps from '@/generated/props/SwitchProps';

export default ({ children }) => (
  <DocsLayout
    title="Switch — Cladd"
    description="Switch component props."
  >
    {children}
  </DocsLayout>
);

# Switch

## Props

<SwitchProps />
