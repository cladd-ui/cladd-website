import { DocsLayout } from '@/components/DocsLayout';
import TextareaProps from '@/generated/props/TextareaProps';

export default ({ children }) => (
  <DocsLayout title="TextArea — Cladd" description="TextArea component props.">
    {children}
  </DocsLayout>
);

# TextArea

## Props

<TextareaProps />
