import { DocsLayout } from '@/components/DocsLayout';
import ListTitleProps from '@/generated/props/ListTitleProps';

export default ({ children }) => (
  <DocsLayout
    title="ListTitle — Cladd"
    description="ListTitle component props."
  >
    {children}
  </DocsLayout>
);

# ListTitle

## Props

<ListTitleProps />
