import { DocsLayout } from '@/components/DocsLayout';
import BackdropProps from '@/generated/props/BackdropProps';

export default ({ children }) => (
  <DocsLayout title="Backdrop — Cladd" description="Backdrop component props.">
    {children}
  </DocsLayout>
);

# Backdrop

## Props

<BackdropProps />
