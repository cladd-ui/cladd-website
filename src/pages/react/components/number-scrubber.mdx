import { DocsLayout } from '@/components/DocsLayout';
import NumberScrubberProps from '@/generated/props/NumberScrubberProps';

export default ({ children }) => (
  <DocsLayout
    title="NumberScrubber — Cladd"
    description="NumberScrubber component props."
  >
    {children}
  </DocsLayout>
);

# NumberScrubber

## Props

<NumberScrubberProps />
