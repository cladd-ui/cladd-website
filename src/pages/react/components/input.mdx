import { DocsLayout } from '@/components/DocsLayout';
import InputProps from '@/generated/props/InputProps';

export default ({ children }) => (
  <DocsLayout title="Input — Cladd" description="Input component props.">
    {children}
  </DocsLayout>
);

# Input

## Props

<InputProps />
