import { DocsLayout } from '@/components/DocsLayout';
import ChipProps from '@/generated/props/ChipProps';

export default ({ children }) => (
  <DocsLayout
    title="Chip — Cladd"
    description="Chip component props."
  >
    {children}
  </DocsLayout>
);

# Chip

## Props

<ChipProps />
