import { DocsLayout } from '@/components/DocsLayout';
import OTPFieldProps from '@/generated/props/OTPFieldProps';

export default ({ children }) => (
  <DocsLayout title="OTPField — Cladd" description="OTPField component props.">
    {children}
  </DocsLayout>
);

# OTPField

## Props

<OTPFieldProps />
