import { DocsLayout } from '@/components/DocsLayout';
import SliderProps from '@/generated/props/SliderProps';

export default ({ children }) => (
  <DocsLayout title="Slider — Cladd" description="Slider component props.">
    {children}
  </DocsLayout>
);

# Slider

## Props

<SliderProps />
