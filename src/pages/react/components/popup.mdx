import { DocsLayout } from '@/components/DocsLayout';
import PopupProps from '@/generated/props/PopupProps';

export default ({ children }) => (
  <DocsLayout title="Popup — Cladd" description="Popup component props.">
    {children}
  </DocsLayout>
);

# Popup

## Props

<PopupProps />
