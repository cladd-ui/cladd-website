import { DocsLayout } from '@/components/DocsLayout';
import ToolbarProps from '@/generated/props/ToolbarProps';

export default ({ children }) => (
  <DocsLayout title="Toolbar — Cladd" description="Toolbar component props.">
    {children}
  </DocsLayout>
);

# Toolbar

## Props

<ToolbarProps />
