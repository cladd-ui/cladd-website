import { DocsLayout } from '@/components/DocsLayout';
import SearchFieldProps from '@/generated/props/SearchFieldProps';

export default ({ children }) => (
  <DocsLayout
    title="SearchField — Cladd"
    description="SearchField component props."
  >
    {children}
  </DocsLayout>
);

# SearchField

## Props

<SearchFieldProps />
