import { DocsLayout } from '@/components/DocsLayout';
import PopoverProps from '@/generated/props/PopoverProps';

export default ({ children }) => (
  <DocsLayout title="Popover — Cladd" description="Popover component props.">
    {children}
  </DocsLayout>
);

# Popover

## Props

<PopoverProps />
