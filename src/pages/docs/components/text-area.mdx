import { DocsLayout } from '@/components/DocsLayout';
import TextAreaProps from '@/generated/props/TextAreaProps';

export default ({ children }) => (
  <DocsLayout
    title="TextArea — Cladd"
    description="TextArea component props."
  >
    {children}
  </DocsLayout>
);

# TextArea

## Props

<TextAreaProps />
