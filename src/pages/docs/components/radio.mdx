import { DocsLayout } from '@/components/DocsLayout';
import RadioProps from '@/generated/props/RadioProps';

export default ({ children }) => (
  <DocsLayout
    title="Radio — Cladd"
    description="Radio component props."
  >
    {children}
  </DocsLayout>
);

# Radio

## Props

<RadioProps />
