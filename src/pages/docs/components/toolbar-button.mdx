import { DocsLayout } from '@/components/DocsLayout';
import ToolbarButtonProps from '@/generated/props/ToolbarButtonProps';

export default ({ children }) => (
  <DocsLayout
    title="ToolbarButton — Cladd"
    description="ToolbarButton component props."
  >
    {children}
  </DocsLayout>
);

# ToolbarButton

## Props

<ToolbarButtonProps />
