import { DocsLayout } from '@/components/DocsLayout';
import TooltipPrimitiveProps from '@/generated/props/TooltipPrimitiveProps';

export default ({ children }) => (
  <DocsLayout
    title="TooltipPrimitive — Cladd"
    description="TooltipPrimitive component props."
  >
    {children}
  </DocsLayout>
);

# TooltipPrimitive

## Props

<TooltipPrimitiveProps />
