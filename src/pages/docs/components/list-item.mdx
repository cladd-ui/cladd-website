import { DocsLayout } from '@/components/DocsLayout';
import ListItemProps from '@/generated/props/ListItemProps';

export default ({ children }) => (
  <DocsLayout
    title="ListItem — Cladd"
    description="ListItem component props."
  >
    {children}
  </DocsLayout>
);

# ListItem

## Props

<ListItemProps />
