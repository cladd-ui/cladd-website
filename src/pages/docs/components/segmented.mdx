import { DocsLayout } from '@/components/DocsLayout';
import SegmentedProps from '@/generated/props/SegmentedProps';

export default ({ children }) => (
  <DocsLayout
    title="Segmented — Cladd"
    description="Segmented component props."
  >
    {children}
  </DocsLayout>
);

# Segmented

## Props

<SegmentedProps />
