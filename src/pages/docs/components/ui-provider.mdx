import { DocsLayout } from '@/components/DocsLayout';
import UIProviderProps from '@/generated/props/UIProviderProps';

export default ({ children }) => (
  <DocsLayout
    title="UIProvider — Cladd"
    description="UIProvider component props."
  >
    {children}
  </DocsLayout>
);

# UIProvider

## Props

<UIProviderProps />
