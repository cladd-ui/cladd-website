import { DocsLayout } from '@/components/DocsLayout';
import SelectProps from '@/generated/props/SelectProps';

export default ({ children }) => (
  <DocsLayout
    title="Select — Cladd"
    description="Select component props."
  >
    {children}
  </DocsLayout>
);

# Select

## Props

<SelectProps />
