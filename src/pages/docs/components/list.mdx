import { DocsLayout } from '@/components/DocsLayout';
import ListProps from '@/generated/props/ListProps';

export default ({ children }) => (
  <DocsLayout
    title="List — Cladd"
    description="List component props."
  >
    {children}
  </DocsLayout>
);

# List

## Props

<ListProps />
