import { DocsLayout } from '@/components/DocsLayout';
import ListButtonProps from '@/generated/props/ListButtonProps';

export default ({ children }) => (
  <DocsLayout
    title="ListButton — Cladd"
    description="ListButton component props."
  >
    {children}
  </DocsLayout>
);

# ListButton

## Props

<ListButtonProps />
