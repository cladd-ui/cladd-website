import { DocsLayout } from '@/components/DocsLayout';
import SurfaceProps from '@/generated/props/SurfaceProps';

export default ({ children }) => (
  <DocsLayout
    title="Surface — Cladd"
    description="Surface component props."
  >
    {children}
  </DocsLayout>
);

# Surface

## Props

<SurfaceProps />
