import { DocsLayout } from '@/components/DocsLayout';
import TooltipProps from '@/generated/props/TooltipProps';

export default ({ children }) => (
  <DocsLayout
    title="Tooltip — Cladd"
    description="Tooltip component props."
  >
    {children}
  </DocsLayout>
);

# Tooltip

## Props

<TooltipProps />
