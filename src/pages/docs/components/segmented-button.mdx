import { DocsLayout } from '@/components/DocsLayout';
import SegmentedButtonProps from '@/generated/props/SegmentedButtonProps';

export default ({ children }) => (
  <DocsLayout
    title="SegmentedButton — Cladd"
    description="SegmentedButton component props."
  >
    {children}
  </DocsLayout>
);

# SegmentedButton

## Props

<SegmentedButtonProps />
