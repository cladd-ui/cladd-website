import { DocsLayout } from '@/components/DocsLayout';
import ShortcutProps from '@/generated/props/ShortcutProps';

export default ({ children }) => (
  <DocsLayout
    title="Shortcut — Cladd"
    description="Shortcut component props."
  >
    {children}
  </DocsLayout>
);

# Shortcut

## Props

<ShortcutProps />
