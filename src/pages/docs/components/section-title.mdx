import { DocsLayout } from '@/components/DocsLayout';
import SectionTitleProps from '@/generated/props/SectionTitleProps';

export default ({ children }) => (
  <DocsLayout
    title="SectionTitle — Cladd"
    description="SectionTitle component props."
  >
    {children}
  </DocsLayout>
);

# SectionTitle

## Props

<SectionTitleProps />
