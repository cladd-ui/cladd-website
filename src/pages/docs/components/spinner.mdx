import { DocsLayout } from '@/components/DocsLayout';
import SpinnerProps from '@/generated/props/SpinnerProps';

export default ({ children }) => (
  <DocsLayout
    title="Spinner — Cladd"
    description="Spinner component props."
  >
    {children}
  </DocsLayout>
);

# Spinner

## Props

<SpinnerProps />
